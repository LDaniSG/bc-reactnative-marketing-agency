# Semana 01 — Core Components y Flexbox Layout

> **Bootcamp React Native (SENA)** | **Dominio Asignado**: Agencia de Marketing Digital (`Apex Media Agency`)
> **Aprendiz**: LDaniSG | **Entidades**: `clients`, `campaigns`, `metrics`, `channels`

---

## 📋 Requisitos Cumplidos (Semana 01)
- **Core Components**: Uso de `View`, `Text`, `Image`, `Pressable` y `SafeAreaView`.
- **Flexbox Layout**: Distribución responsive con Flexbox.
- **StyleSheet.create**: Estilos organizados en `src/theme/index.ts`.
- **Adaptación al Dominio**: Tarjetas de campañas publicitarias en español.
- **TypeScript Estricto**: Tipado completo sin `any`.

---

## 🚀 Ejecución
```bash
npm install
npx expo start --web
```
